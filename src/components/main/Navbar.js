import React from 'react'

const Navbar = (props) => {
    return (
        <div style={{'align': `${props.orientation}`}}>
            This is the Navbar
        </div>
    )
}

export default Navbar